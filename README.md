# Hi there, I'm Carla Jane! 👋🐾

**💖 Coding? More like *"trying"* to code!**  
**🎀 Pink is my favorite error message color**  

---

### 🔮 My Tech Stack (aka Things I Google Daily)
```python
def my_skills():
    skills = {
        'languages': ['Python', 'JavaScript', 'HTML/CSS (kinda)'],
        'tools': ['Git', 'VS Code', 'Ctrl + C / Ctrl + V'],
        'frameworks': ['React (if it behaves)', 'Bootstrap (pls work)'],
        'databases': ['SQL (SELECT * FROM my_brain WHERE knowledge > 0)']
    }
    return "✨ Magic ✨" if skills == {} else skills

const mood = ["dancing", "crying over code", "watching memes", "napping"];
console.log(mood[Math.floor(Math.random() * mood.length)]);
// Output: "Why is this undefined?!"
